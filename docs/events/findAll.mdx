import Tabs from '@theme/Tabs'
import TabItem from '@theme/TabItem'
import CodeBlock from '@theme/CodeBlock'
import confetti from 'confetti'
import Parameters from '@site/src/components/Parameters'

# Find all

<Parameters model={confetti.models.event} />

<Tabs>
  <TabItem value="javascript" label="JavaScript" default>

    ```js  
    const Confetti = require('confetti')

    const confetti = new Confetti({ apiKey: 'your-key' })
    const events = await confetti.events.findAll({
      filter: {
        signupType: 'rsvp',
      },
      page: {
        size: 10,
        number: 3,
      },
    })
    ```
    <CodeBlock language="javascript" title="Result">
      {`${JSON.stringify(confetti.models.event.sample.multiple.formatted, null, 2)}`}
    </CodeBlock>

  </TabItem>
  <TabItem value="curl" label="curl">

    ```shell
    curl "https://api.confetti.events/events?filter[signupType]=rsvp"
      -H "Authorization: apikey your-key"
    ```

    <CodeBlock language="javascript" title="Result">
      {`${JSON.stringify(confetti.models.event.sample.multiple.raw, null, 2)}`}
    </CodeBlock>
    
  </TabItem>
</Tabs>


