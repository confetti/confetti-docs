import Tabs from '@theme/Tabs'
import TabItem from '@theme/TabItem'
import CodeBlock from '@theme/CodeBlock'
import confetti from 'confetti'
import Parameters from '@site/src/components/Parameters'

# Find one

<Parameters model={confetti.models.ticket} includeOnly={true} />

<Tabs>
  <TabItem value="javascript" label="JavaScript" default>

    ```js  
    const Confetti = require('confetti')

    const confetti = new Confetti({ apiKey: 'your-key' })
    const ticket = await confetti.tickets.find(2)
    ```
    <CodeBlock language="javascript" title="Result">
      {`${JSON.stringify(confetti.models.ticket.sample.single.formatted, null, 2)}`}
    </CodeBlock>

  </TabItem>
  <TabItem value="curl" label="curl">

    ```shell
    curl "https://api.confetti.events/tickets/2"
      -H "Authorization: apikey your-key"
    ```

    <CodeBlock language="javascript" title="Result">
      {`${JSON.stringify(confetti.models.ticket.sample.single.raw, null, 2)}`}
    </CodeBlock>
    
  </TabItem>
</Tabs>


